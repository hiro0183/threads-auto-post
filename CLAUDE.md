# CLAUDE.md — コンサル垢（ヒロ先生）実行ルールブック

> **⚠️ 2026-09-18 大転換。この垢の役割が変わりました。読み飛ばさないでください。**

このリポジトリは **コンサル垢（ヒロ先生・@hiro_nariai_salon_）専用**。
ラポール整体院（threads_tool_rapport / @rapport.sango）とは別物。コードもテーマも口調も絶対に混同しない。

---

## 🔴 いちばん大事な変更（2026-09-18・なりあいさん決定）

**この垢は「集客エンジン」をやめて「紹介状」になりました。**

BNIや1to1で会った社長が、あとで「この人に頼んでいいか」を確かめに来る場所です。
フォロワーもviewsも追いません。追うのは「直近20投稿が名刺として読めるか」だけ。

| | 旧（〜2026-09-17） | 新（2026-09-18〜） |
|:--|:--|:--|
| 投稿 | 1日24本 | **週5本（月〜金・07:30の1枠）** |
| 向ける相手 | 整骨院の先生 | **BNIや1to1で会った社長** |
| 原稿づくり | 週次企画（日曜04:06）＋日次執筆（毎朝06:00） | **月1回20本まとめて** |
| 検品 | 毎日12:00 | **月1回** |
| 書き方の正本 | prompts/ 11ファイル | **`prompts/writing.md` 1枚** |
| 追う数字 | views・フォロワー・ER・到達率 | **20投稿が名刺として読めるか（月1回・本人）** |

---

## 🛑 日次・週次の自動処理は止めました

**クラウドルーティン（週次企画・日次執筆・独立検品）が起動しても、何もせず終了してください。**
役割が変わり、日次で書くものがありません。原稿は月1回まとめて作ります。

**2026-09-18に、クラウド側で実際に止めました。**

| ルーティン | 旧 | いま |
|:--|:--|:--|
| コンサルThreads 日次投稿生成（毎朝04:40 UTC） | 毎日24枠を執筆 | **停止（enabled: false）** |
| コンサルThreads 週次企画（日曜04:00 UTC） | 毎週168フックを企画 | **停止（enabled: false）** |
| 毎日の検品（両垢・12:00 JST） | 両垢を検品 | **ラポール垢だけ検品。コンサル垢は原稿を読むだけ** |
| コンサルThreads 到達率チェック | 24枠の欠けを監視 | 稼働のまま（`reach_check.py` は原稿が無い日を「休み」扱いに修正済み） |

> 再開するときは claude.ai の Routines で enabled を戻します（このファイル末尾「戻すとき」）。
> もし止め忘れたルーティンが起動したら、**何もせず終了してください。**
> 費用を使わないこと、原稿を勝手に増やさないことが目的です。

止めたもの: 週次レポート／実験台帳／研究部門（research_digest）／絡み部門（engage）／
到達率モニタ／司令室の赤／5部門制。すべて1日24本を管理するための仕組みでした。

---

## 月1回の手順（これだけ）

**月末に、翌月の20本（週5本 × 4週）をまとめて作ります。**

1. **なりあいさんが題材を5つ選ぶ（30分）**
   この1ヶ月のBNI・1to1で社長から実際に出た困りごと。**ここが唯一の人間の仕事**
2. **生成（Sonnet）**: `prompts/writing.md` のとおりに20本。背骨1と背骨2から各10本
3. **機械検査**: ⚠️ **いまの `check_hooks.py` は新しい形式を検査できません**（旧の週次プラン `days` 構造を前提にしていて、無いと何もせず正常終了します）。`check_body_style.py` も1日1枠だと常に100%でOKになります。**月1回の生成では、20本を1つのファイルにまとめて検査する形に作り直すのが宿題です**（2026-09-18時点で未着手）。それまでは §5 の9項目を人とHaikuが見るのが唯一の砦です
4. **独立検品（Haiku）**: 別セッションで `writing.md` §5 の9項目だけを見る。生成側の意図は渡さない
5. **なりあいさんが20本を通して読む（15分）**
6. `posts/YYYY-MM-DD.json` に1日1枠（`"07:30"`）で置く。**土日は置かない**

**なりあいさんの作業は月45分。毎日・毎週の判断はありません。**

---

## posts/{date}.json のフォーマット

**実際に `post_runner.py` が読む形は、スロット名 → 投稿本文の配列です。** オブジェクトではありません
（2026-09-18に旧CLAUDE.mdの記載が実コードと食い違っていたのを修正しました。
`load_scheduled_post()` は `schedule.get(slot)` をそのまま投稿本文の配列として使います）。

```json
{
  "07:30": [
    "【1】フック（20〜30字）",
    "【2】中身",
    "【3】まとめ"
  ]
}
```

- 1本＝3連ツリー。**単体投稿は禁止**
- 原稿が無い日は投稿しません（`scheduler.py` が `[NO-DRAFT]` を残して何もしない・2026-07-28の方針）。
  **土日に原稿が無いのは正常です**
- 土日はファイルを作らない＝自動的に休む

## 書き方

**`prompts/writing.md` が唯一の正本です。** 旧11ファイル（persona / hook_rules / body_rules /
taboo / gate / funnel_rules / weekly_plan_routine / daily_writing_routine / research_digest /
theme_shelf / engage_rules）はこの1枚に統合し、`archive/prompts_20260918_kaisojo/` に保存しました。

別ファイルのまま残すもの:
- `prompts/stories.md` — 素材帳（本人の実話と数字）。**ここに無いことは書かない**
- `prompts/profile.md` — 名乗り・bio・固定投稿・飛び先
- `prompts/lp_copy_v2.md` / `prompts/line_steps_v2.md` — LP・LINE導線の正本（Threads投稿とは別）
- `prompts/ig_story_rules.md` / `prompts/ig_weekly_plan_routine.md` — IGストーリー（別チャネル・別判断）
- `prompts/reel_scripts.md` — リール（別案件）

---

## 絶対に破らない9つ（`writing.md` §5 の写し）

1. `stories.md`・`profile.md` に無い数字と実話は書かない
2. 事実に理由を足さない（本人の年数に因果を付けない）
3. 「保険」の語を出さない
4. 地名「辻堂」を出さない
5. 単体投稿を出さない（必ず3連）
6. 売り込みを本文に入れない。CTAは週1本（金曜）まで。商品名・価格は書かない
7. AIを主語にしたフックを書かない
8. 他人の不幸を見物させる型を使わない
9. 同じ数字は月3回まで。同じ場面は月1回まで

**使用禁止の数字:** 8,500円／1,500円／46歳／「150から160」／250万→100万の失敗談／450万・500万・850万

---

## スロット設計

`post_runner.py` の `SLOT_PLAN` が唯一の正。**現在は07:30の1枠だけ**。
`POST_SCHEDULE` はそこから機械的に導出されるので、ズレることは構造上ありません。
旧24枠は `post_runner.py.bak_24slots_20260918` にあります。

---

## エスカレーション（自分で直さず上に上げる）

- `stories.md` に無い数字や実話が必要になったとき → **創作しない**。なりあいさんに聞く
- 題材が5つに足りないとき → 埋めない。本数を減らす
- 投稿が出ていないとき → `python reach_check.py` で実物を見てから報告

---

## 壊れたら最初に疑うところ

- 投稿が出ない → `python reach_check.py` → `SLOT_PLAN`と`posts/{date}.json`のキーの一致
- 実測が止まった → `python collect_insights.py`（Threads API起点）
- 同期が止まった → `python ingest_render.py`（Renderから取りに行く向き）

---

## 運用メモ

- **投稿実行はRender（scheduler.py）が担当。** このセッションでは投稿APIを直接叩かない
- トークンはDB永続化済み（`db_state.threads_token`）。再デプロイで巻き戻らない
- 手動再認証の直後だけ `python token_manager.py --seed`
- 生成・検品は**サブスク完結・API課金ゼロ**（`ANTHROPIC_API_KEY` を環境に残さない）
- **モデル:** 生成と整形は Sonnet／検品は Haiku／**仕組みを直すのは Fable か Opus**（月1回・手動起動）
- 旧ルールブックは `CLAUDE.md.bak_20260918` にあります

---

## 戻すとき

役割を集客エンジンに戻す判断をした場合:
1. `post_runner.py.bak_24slots_20260918` から SLOT_PLAN を戻す
2. `archive/prompts_20260918_kaisojo/` から旧11ファイルを `prompts/` へ戻す
3. `CLAUDE.md.bak_20260918` を戻す
4. クラウドルーティン3本を再開する
